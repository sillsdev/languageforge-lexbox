# this file is only here so it can be referenced by the action.yml file
# as it requires a dockerfile and will not accept an image tag,
# the actual image is built from the Dockerfile in this folder
FROM ghcr.io/sillsdev/lexbox-dotnet-integration-runner:latest
