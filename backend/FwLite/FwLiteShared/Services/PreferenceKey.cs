namespace FwLiteShared.Services;

/// <summary>
/// Keys for app-level preferences stored via IPreferencesService.
/// Exported to TypeScript via Reinforced.Typings so both sides share the same constants.
/// </summary>
public enum PreferenceKey
{
    AppLastUrl,
}
