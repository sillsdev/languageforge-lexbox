# Label

Accessible label for a form control. Svelte: `import { Label } from '$lib/components/ui/label';`

**Class recipe**: `flex items-center gap-2 text-sm leading-none font-medium select-none`. Pair with a control via `for` / `id`.
