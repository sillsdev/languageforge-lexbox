# Badge

Compact status / category label. Svelte: `import { Badge } from '$lib/components/ui/badge';`

**Props**: `variant` = `default | secondary | destructive | outline` · `href` (renders an anchor).

**Class recipe**: `inline-flex items-center gap-1 rounded-full border px-2 py-0.5 text-xs font-medium` + variant colors (default = `bg-primary text-primary-foreground border-transparent`).
