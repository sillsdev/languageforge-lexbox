import Root, {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
} from './button.svelte';

import XButton from './x-button.svelte';

export {
  Root,
  XButton,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  //
  Root as Button,
  type ButtonProps as Props
};
