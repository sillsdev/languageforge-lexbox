@tailwind base;
@tailwind components;
@tailwind utilities;

/* frontend/src/lib/forms/Form.svelte */
form {
  input {
    @apply mb-4;
  }

  .btn {
    @apply mt-4;
  }

  .input-error {
    @apply mb-0;
  }
}

.overflow-wrap {
  overflow-wrap: break-word;
  overflow: hidden;
}

.button-badge[tabindex='-1'] {
  @apply hover:drop-shadow-xl hover:brightness-95 transition cursor-pointer;
}

form {
  input,
  select,
  textarea {
    margin: 0;
  }

  .form-control {
    @apply mb-2;
  }
}

.toast:where(.toast-center) {
  right: unset;
  white-space: unset;
}

.delete-modal .btn[type='submit'] {
  @apply btn-error;
}

.menu .text-error {
  @apply !text-error;
}

/* rescues a border that vanishes in FireFox */
@supports (-moz-appearance: none) {
  .table thead tr:first-child.sticky {
    border-bottom-width: 2px;
  }
}
